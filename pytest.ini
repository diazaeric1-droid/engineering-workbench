[pytest]
# Collect ONLY the product's own tests. The vendored component repos under apps/
# carry their own test suites (written against a top-level `src` package that does
# not exist in this repo's import space) — they run in their home repos' CI.
testpaths = tests
norecursedirs = apps .venv .git
