.env
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.venv/
venv/
dist/
build/
*.egg-info/
.streamlit/secrets.toml
# Workbench vendoring (v0.2.0): the trained model + synthetic SCADA are COMMITTED so
# the consolidated product loads with no first-run training. They are deterministic
# (seed 7 / fixed-seed model), so shipping them costs nothing in reproducibility.
#   (was ignored: artifacts/  ·  data/synthetic/*.csv  ·  data/synthetic/labels.csv)
.DS_Store
