# environment / caches
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/
.DS_Store
.env
.streamlit/secrets.toml

# ---- deterministic synthetic artifacts are now COMMITTED (v0.2.0) ----
# They were previously regenerated by core.bootstrap() on every cold start, which
# added a ~30 s ESP-training step + a "first-time setup" banner on load. They are
# fully deterministic (ESP SCADA seed 7, gas-lift fleet seed 42, fixed-seed model),
# so committing them removes that cold-start cost with no loss of reproducibility.
# core.bootstrap() still regenerates them on an environment that is missing them.
#   committed: apps/esp-failure-risk-agent/artifacts/  (esp_risk_model.joblib + training_report.json)
#   committed: apps/esp-failure-risk-agent/data/synthetic/  (SCADA well_*.csv + labels.csv)
#   committed: apps/well-gas-lift-advisor/data/synthetic/  (fleet/ + ground_truth.csv)
apps/well-gas-lift-advisor/evals/optimization_report.json

# production-engineer-copilot: user-supplied NDIC extract + eval scratch.
# (its synthetic JSONs + the real Colorado CSV are COMMITTED, exactly as upstream;
# the four scored eval summaries stay committed — the honest-eval chip reads them.
# `results/*` + negations rather than `results/` so the re-includes can take effect.)
apps/production-engineer-copilot/data/real/ndic/production.csv
apps/production-engineer-copilot/evals/results/*
!apps/production-engineer-copilot/evals/results/summary.json
!apps/production-engineer-copilot/evals/results/model_frontier.json
!apps/production-engineer-copilot/evals/results/adversarial.txt
!apps/production-engineer-copilot/evals/results/holdout
apps/production-engineer-copilot/evals/results/holdout/*
!apps/production-engineer-copilot/evals/results/holdout/summary_holdout.json
